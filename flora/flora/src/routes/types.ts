type selectedFieldsOptions = {
    lifecycle: boolean,
    introduced: boolean,
    endemic: boolean,
    synonyms: boolean,
    first_observation_date: boolean,
    last_observation_date: boolean
}

export type {selectedFieldsOptions}
